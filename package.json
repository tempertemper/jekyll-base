{
  "name": "jekyll-base",
  "version": "1.0.0",
  "description": "This is the starting point for Jekyll projects",
  "main": "index.js",
  "scripts": {
    "serve": "bundle exec jekyll clean && bundle exec jekyll serve --watch --incremental",
    "build": "bundle exec jekyll clean && bundle exec jekyll build",
    "staging": "npm run build && echo 'User-agent: *\nDisallow: /' > dist/robots.txt",
    "start": "npm run serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tempertemper/jekyll-base.git"
  },
  "author": "Martin Underhill",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tempertemper/jekyll-base/issues"
  }
}
